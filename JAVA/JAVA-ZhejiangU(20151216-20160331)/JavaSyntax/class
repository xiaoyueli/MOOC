#class

1. 包括成员变量和成员方法

2. private/public/friendly/protected

3. 缺省类型的成员变量和方法是friendly的，friendly的成员变量
和方法可以被处于相同的包的类访问。

4. static是类函数和类变量的关键字，类函数和类变量不属于任何对象
而属于整个类，可以通过类名和对象名.访问，任何对象访问的值都一样。
	类函数不属于任何对象，所以不能访问任何非static的成员方法和
变量，必须通过建立对象的方式访问或者访问static方法和变量。
	static变量只会初始化一次，与对象的建立无关，即在类的装载时
初始化。